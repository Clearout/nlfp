<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>no.dsb.bris2</groupId>
    <artifactId>bris2</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>1.5.10.RELEASE</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>1.8</java.version>
        <!-- Dependency versions: -->
        <spring-boot-admin.version>1.5.4</spring-boot-admin.version>
        <springfox-swagger.version>2.7.0</springfox-swagger.version>
        <swagger.version>1.5.10</swagger.version>
        <commons-io.version>2.5</commons-io.version>
        <commons-text.version>1.1</commons-text.version>
        <validation-api.version>1.1.0.Final</validation-api.version>
        <jasypt-spring-boot.version>1.16</jasypt-spring-boot.version>
        <javamelody.version>1.69.0</javamelody.version>
        <logstash-logback-encoder.version>4.11</logstash-logback-encoder.version>
        <spring-cloud-security.version>1.2.1.RELEASE</spring-cloud-security.version>
        <com.google.auto.value.version>1.3</com.google.auto.value.version>
        <!--  Override spring boot version of Jackson because of CVE-2017-17485. Can be removed when spring upgrades to 2.8.11 or newer  -->
        <jackson.version>2.8.11</jackson.version>
    </properties>

</project>
